package com.techgrounds.netflix.dto;

public class TMDBActorDTO {
//    Add names of actors from TMDB endpoint /movie/{movie_id}/credits Only names.
//    cast is an array. Add only names from that array, but all actors
}
